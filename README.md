# CarInsight Frontend 🚗

Interface de usuário da plataforma **CarInsight**, desenvolvida para consumo de dados em tempo real.

## 🚀 Funcionalidades
- **Integração Real**: Conectado à API REST (NestJS) para buscar dados do banco PostgreSQL.
- **Renderização Dinâmica**: Substituição de cards estáticos por dados reais do estoque.
- **Scraping Data**: Exibe veículos importados automaticamente de fontes externas (Ex: Renatinhu's Cars).

## 🛠 Tech Stack
- **HTML5 / CSS3 / JavaScript (Vanilla)**
- **Fetch API** para comunicação com Backend.
- **Bootstrap 3.x** (Legado mantido para compatibilidade visual).

## 📦 Instalação
1. Clone o repositório.
2. Servir com qualquer servidor estático (ex: `npx serve`).
   ```bash
   npx serve -l 8080
   ```
3. Certifique-se que o backend CarInsight está rodando na porta 3000.
