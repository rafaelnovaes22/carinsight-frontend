{
  "name": "carinsight-frontend",
  "version": "1.0.0",
  "description": "Frontend for CarInsight",
  "main": "index.html",
  "scripts": {
    "start": "serve . -l 8080",
    "setup": "node scripts/setup.mjs",
    "test": "playwright test",
    "test:unit": "node --test tests-unit/*.test.mjs",
    "typecheck": "tsc -p jsconfig.json",
    "format": "prettier --write discovery api-origin.mjs scripts tests-unit AGENTS.md README.md package.json playwright.config.ts jsconfig.json .github",
    "format:check": "prettier --check discovery api-origin.mjs scripts tests-unit AGENTS.md README.md package.json playwright.config.ts jsconfig.json .github",
    "verify": "npm run format:check && npm run typecheck && npm run test:unit && npm test"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "keywords": [],
  "author": "Rafael Novaes",
  "license": "ISC",
  "engines": {
    "node": ">=20.19 <25"
  },
  "devDependencies": {
    "@playwright/test": "1.57.0",
    "prettier": "3.6.2",
    "typescript": "5.9.3"
  }
}
