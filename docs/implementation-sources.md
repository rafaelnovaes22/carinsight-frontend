# Fontes de implementação

- Fetch, status HTTP e cancelamento: https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch
- JavaScript tipado incrementalmente: https://www.typescriptlang.org/tsconfig/checkJs.html
- Servidor de desenvolvimento isolado nos E2E: https://playwright.dev/docs/test-webserver
- Contrato de descoberta alinhado com o backend nesta branch: `POST /discovery/recommendations`, `naturalLanguage`, `constraints`, `limit`; resposta com `status`, `recommendations`, `summary`, `followUpQuestions`, `inventoryCount` e `interpretation`.

Direção de arte: a clareza azul do CarInsight existente, com um briefing central que se transforma em escolhas justificadas. Manter logo, assets, fundo claro e tipografia; eliminar demonstrações de inventário apresentadas como disponibilidade real.
